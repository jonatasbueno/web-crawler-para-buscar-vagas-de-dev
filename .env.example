SLACK_WEBHOOK_URL=
# Opcional: token do GitHub para elevar o limite da API de 60 para 5000 req/h
# (usado pelos scrapers dos repositórios */vagas). Sem ele, funciona não autenticado.
GITHUB_TOKEN=
# Opcional: empresas no Quickin a coletar (slugs separados por vírgula). Sem isto, usa a lista padrão.
